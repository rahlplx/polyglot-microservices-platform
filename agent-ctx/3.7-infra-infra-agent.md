---
Task ID: 3.7-infra
Agent: Infra Agent
Project Phase: 3 (Implementation & Vibe Coding)
Task: Create Kubernetes infrastructure manifests for polyglot microservices architecture

Work Log:
- Read worklog.md, PHASE2_DESIGN_SPEC.md, PHASE3_IMPLEMENTATION_SPEC.md, and docs/performance-budget.md for context
- Created directory structure: infra/kubernetes/{base,platform,apps,overlays/{dev,staging,production}} and .github/workflows/
- Created base/namespace.yaml: production namespace with restricted pod security labels, ArgoCD managed-by label
- Created base/serviceaccount.yaml: 9 ServiceAccounts (gateway, identity, catalog, order, payment, notification, analytics, cdc-relay, schema-registry) each with SPIFFE ID annotation (spiffe://trust.example.org/ns/production/sa/{name})
- Created base/networkpolicy.yaml: Default-deny ingress+egress, DNS allow, per-service explicit allow rules for all inter-service communication paths (gateway→backends, order→catalog/payment/notification, Kafka→consumers, OTel/SPIRE platform access)
- Created platform/otel-collector.yaml: DaemonSet (1 per node), ConfigMap with OTLP/gRPC+HTTP receivers, tail_sampling processor (errors=100%, slow>2s=100%, critical paths=100%, baseline=10%), batch+resource processors, OTLP exporter to Gateway, Prometheus self-monitoring
- Created platform/otel-gateway.yaml: Deployment (2 replicas, HA), fan-out to Prometheus (metrics), Tempo (traces), Loki (logs), HPA (2-6 replicas), pod anti-affinity across zones
- Created platform/spire-server.yaml: StatefulSet (3 replicas), ConfigMap with server.conf (trust domain trust.example.org, K8s PSAT node attestation, SQLite DataStore, disk KeyManager, k8sbundle Notifier), bundle ConfigMap, headless Service
- Created platform/spire-agent.yaml: DaemonSet (1 per node), ConfigMap with agent.conf, K8s PSAT attestation, Unix workload attestation, Workload API via host socket (/run/spire/sockets), projected SA token for SPIRE server auth
- Created platform/kafka.yaml: Zookeeper StatefulSet (3 replicas) + Kafka StatefulSet (3 brokers, replication factor 3, min.insync.replicas=2, 12 partitions), headless Services
- Created platform/debezium.yaml: Deployment (1 replica, Recreate strategy), ConfigMap with Kafka Connect config, JMX Exporter sidecar for OTel metrics, PostgreSQL WAL capture via secrets
- Created apps/app-of-apps.yaml: ApplicationSet (list generator for 9 services), AppProject with RBAC, platform-components Application for OTel/SPIRE/Kafka/Debezium
- Created apps/{gateway,identity,catalog,order,payment,notification,analytics,cdc-relay,schema-registry}.yaml: Individual ArgoCD Application manifests per service with Kustomize source, auto-sync, retry policy
- Created overlays/dev/kustomization.yaml: 1 replica per service, 50% reduced resources, DEBUG logging, SPIRE trust.dev.example.org, OTel always_on sampler, single Kafka/Zookeeper
- Created overlays/staging/kustomization.yaml: 2 replicas, 80% production resources, INFO logging, SPIRE trust.staging.example.org, 50% trace sampling, full Kafka cluster
- Created overlays/production/kustomization.yaml: Full production budgets from performance-budget.md (Gateway 100m/500m/128Mi/512Mi, Order 500m/2000m/512Mi/2Gi, etc.), topology spread constraints across zones, INFO logging, 10% trace sampling
- Created .github/workflows/schema-ci.yml: Buf lint, Buf breaking change detection against base branch, proto consistency (generated code freshness), OpenAPI spec validation via Redocly, Pact contract verification, summary gate
- Created .github/workflows/service-ci.yml: Reusable workflow with service/language inputs, lint (Go/Rust/TypeScript/Java/Python specific), test with PostgreSQL service container, Docker build+push with SHA/semver tagging, Trivy security scan, ACL enforcement check (proprietary import scanning in domain/inbound layers), OTel instrumentation coverage check (90% target)
- Committed: infra(k8s): add Kubernetes infrastructure manifests for polyglot microservices [P3-3.1]

Spec Items Verified:
- 3.1 Code follows design specification: PASS (all manifests align with PHASE2_DESIGN_SPEC.md Section 2D IaC structure)
- 3.2 All spec items implemented: PASS (no 90% shortcuts — full manifests for all required components)
- Resource budgets aligned with performance-budget.md Section 4: PASS
- SPIFFE/SPIRE identity (spiffe.io format): PASS (all 9 SAs + platform SAs annotated)
- OTel tail-based sampling (errors=100%, slow>2s=100%, critical=100%, baseline=10%): PASS
- Default-deny NetworkPolicy with explicit allow rules per service pair: PASS
- All manifests GitOps-compatible (ArgoCD ApplicationSet + Kustomize overlays): PASS

Stage Summary:
- 21 files created across 5 directories
- Base: 3 files (namespace, 9 SAs, 12 NetworkPolicies)
- Platform: 6 files (OTel DaemonSet+Gateway, SPIRE Server+Agent, Kafka+ZK, Debezium)
- Apps: 10 files (app-of-apps + 9 per-service Applications)
- Overlays: 3 files (dev/staging/production kustomizations)
- CI/CD: 2 files (schema-ci.yml, service-ci.yml)
- All resource budgets match performance-budget.md Section 4
- Commit: 31ad3cd infra(k8s): add Kubernetes infrastructure manifests [P3-3.1]
