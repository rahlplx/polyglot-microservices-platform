# ---------------------------------------------------------------------------
# VPC resources have been moved to ./modules/vpc/
# ---------------------------------------------------------------------------
# All resource, data source, variable, and output blocks previously in this
# file now reside in the VPC module. The root main.tf orchestrates the module
# call and passes the required variables.
# ---------------------------------------------------------------------------
