# ---------------------------------------------------------------------------
# Kubernetes resources have been moved to ./modules/kubernetes/
# ---------------------------------------------------------------------------
# All resource, variable, and output blocks previously in this file now reside
# in the Kubernetes module. The root main.tf orchestrates the module call and
# passes the required variables.
# ---------------------------------------------------------------------------
