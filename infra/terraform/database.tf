# ---------------------------------------------------------------------------
# Database resources have been moved to ./modules/database/
# ---------------------------------------------------------------------------
# All resource, variable, and output blocks previously in this file now reside
# in the Database module. The root main.tf orchestrates the module call and
# passes the required variables.
# ---------------------------------------------------------------------------
